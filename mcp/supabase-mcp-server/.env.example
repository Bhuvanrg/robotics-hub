# Copy to .env and fill in your project values
SUPABASE_URL=
# Prefer service role for full access in controlled local/dev environments
SUPABASE_SERVICE_ROLE_KEY=
# Optional: if no service role provided, you can set SUPABASE_KEY (e.g., anon)
# SUPABASE_KEY=
